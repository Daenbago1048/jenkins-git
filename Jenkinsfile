pipeline {
    agent any
    parameters { choice(name: 'TypeOfProject', choices: ['maven', 'gradle'], description: '') }
    stages {
        stage('Build') {
            steps {
                bat 'set'
            }
        }
    }
}
